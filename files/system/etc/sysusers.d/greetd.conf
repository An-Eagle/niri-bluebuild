# 1. Create the groups first (g = group, - = auto ID)
g video - -
g input - -
g greetd - -

# 2. Create the user (u = user)
u greetd - "greetd user" /var/lib/greetd /sbin/nologin

# 3. Add user to the groups (m = membership)
m greetd video
m greetd input
